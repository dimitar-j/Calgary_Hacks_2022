import React from 'react'

var stylingObjects={
    container:{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "200px",
        background: "#F25044",
        color: "white"
    }
}

// Possibility to add footer. Empty for now
function Footer(){
    return(
        <div>
        </div>
    )
}

export default Footer;